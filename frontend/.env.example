# Launchpad contract address
# Sepolia testnet:
NEXT_PUBLIC_LAUNCHPAD_ADDRESS=0x0130eced40d347abf0ed51bd37f71303c296bc8471bbeba9deb21e03666f2497

# Network ("sepolia" or "mainnet") — controls explorer links
NEXT_PUBLIC_NETWORK=sepolia

# NOTE: starknet.js v2.5.0 uses the feeder gateway (sequencer) protocol.
# The provider is hardcoded to https://alpha-sepolia.starknet.io in manager.ts.
# When a wallet connects, it takes over as the provider automatically.
